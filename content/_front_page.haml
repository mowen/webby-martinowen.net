---
filter: haml
---
- num_of_posts = 8
- posts = find_posts(num_of_posts)
- posts.each_with_index do |post, index|
  - if (index + 1) % 4 == 0
    .last-post-box
      = link_to_page(post)
      %br
      %span.post-date
        = format_post_date_short(post.created_at)
      %p.post-summary
        = post.summary
    - if (index + 1) != num_of_posts
      %hr
  - else 
    .post-box
      = link_to_page(post)
      %br
      %span.post-date
        = format_post_date_short(post.created_at)
      %p.post-summary
        = post.summary
